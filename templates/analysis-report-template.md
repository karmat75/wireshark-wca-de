# Analysebericht: <Titel>

## Zusammenfassung

<Kurze Management- und Technik-taugliche Zusammenfassung in 3 bis 6 Sätzen.>

## Ausgangslage

- Meldung / Symptom:
- Betroffene Systeme:
- Erwartetes Verhalten:
- Beobachtetes Verhalten:

## Capture-Informationen

| Punkt | Wert |
|---|---|
| PCAP-Datei | `<pfad>` |
| Capture-Punkt | `<host/interface/position>` |
| Capture Filter | `<filter oder keiner>` |
| Zeitraum | `<start bis ende>` |
| Analysewerkzeuge | `Wireshark`, `TShark` |
| Bearbeiter | `<name>` |

## Vorgehen

1. `<erster Analyseschritt>`
2. `<zweiter Analyseschritt>`
3. `<dritter Analyseschritt>`

## Verwendete Display Filter

```text
<filter 1>
<filter 2>
<filter 3>
```

## Wichtige Beobachtungen

| Frame | Beobachtung | Bedeutung |
|---:|---|---|
| `<n>` | `<beobachtung>` | `<einordnung>` |
| `<n>` | `<beobachtung>` | `<einordnung>` |
| `<n>` | `<beobachtung>` | `<einordnung>` |

## Bewertung

<Was ist durch den Capture belegt?>

## Einschränkungen

<Was ist nicht belegt?>

Beispiele:

- Der Capture zeigt nur die Sicht am Capture-Punkt.
- Es liegt kein Gegen-Capture vom anderen Ende vor.
- TLS-Inhalte sind nicht entschlüsselt.
- Produktionslogs wurden nicht geprüft.

## Nächste Schritte

1. `<nächster sinnvoller Schritt>`
2. `<weiterer Schritt>`
3. `<optional>`

## TShark-Befehle

```bash
<reproduzierbarer tshark-befehl>
```

```bash
<weiterer tshark-befehl>
```

## Anhang

- Screenshots:
- Exportierte Teil-PCAPs:
- Notizen:
